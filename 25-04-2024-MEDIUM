#include <bits/stdc++.h> 
vector<vector<int>> solve(vector<vector<int>> &arr, int k)
{
    for(int i=0;i<arr.size();i++)
    {
        rotate(arr[i].begin(),arr[i].end()-k,arr[i].end());
    }
    return arr;
}
